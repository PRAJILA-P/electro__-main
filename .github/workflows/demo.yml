name: Normal Static Website

on:
  push:
    branches: [main]

  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: List project files
        run: ls -la

      - name: Build
        run: echo "No build required for a static HTML website."

      - name: Test
        run: echo "No tests available."